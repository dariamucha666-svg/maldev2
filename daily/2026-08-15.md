# Raport C2 — 2026-08-15

## Sesja: nowy serwer C2 TCP (2 porty)

### Cel
Postawienie nowego serwera C2 w wersji TCP: **9999 = agenci**, **9998 = klient CLI**. Test pełnego przepływu z screenshotem.

### Wykonane komendy / kroki
1. **Sprzątanie** — usunięto stare screeny `c2` (5 sesji), zamknięto `nano`, usunięto FIFO `/tmp/c2_ctl.fifo`. Porty 9999/9998 zwolnione.
2. **Zastąpienie server.py** — nowa wersja `~/rat-c2/server.py` (246 linii): dwa gniazda, wątki `agent_loop` (9999) + `cli_loop` (9998), CLI działa po TCP z promptem `C2> `. Backup: `server.py.bak.20260815_054724`.
3. **Uruchomienie** — `screen -dmS c2` (screen `637122.c2`, Detached). Potwierdzone w logu: oba są na `0.0.0.0`.
4. **Testy**:
   - CLI (9998): `list`, `help`, `exit` — OK.
   - Agent (9999): `agent.py` (pil_env, mss) zarejestrowany jako **S0002** (`vserver959630`, 127.0.0.1).
   - `send S0002 screenshot` → `Komenda wysłana, ID: 987689` → wynik odebrany → auto-zapis PNG.
5. **Poprawka `c2cli.py`**:
   - Port: 9999 → **9998** (CLI).
   - Parsowanie screenshotu: pole `data_b64` (zamiast `result`).
   - Timeout w `recv_until_prompt`, by nie wisieć po `exit`.

### Wyniki
- Serwer aktywny: porty **9999** i **9998** nasłuchują.
- Agent online: `S0002 -> vserver959630 (127.0.0.1)`.
- Screenshot: **`/root/screenshot_20260815_054948.png`** (167 239 B, PNG 1200×700 RGB) — poprawny.

### Uwagi / spostrzeżenia
- Sesje liczone globalnie (S0002 przy pierwszym locie) — po restarcie licznik zaczyna od S0001.
- Agent wysyła base64 w polu `data_b64`; klient CLI rozpoznaje je przy auto-zapisie.
- Stary `c2cli.py` wisiał, bo oczekiwał `C2> ` z portu agentów (9999); teraz celem jest 9998.

### Dojutrze / rekomendacje
- Rozważyć dodanie pola `list` porządkującego numerację sesji (autoincrement globalny trwa przez restarty — już resetowany przy starcie modułu, ale `len(clients)` to liczba obecnie aktywnych).
- Można zautomatyzować monitoring agenta .57 (sprawdzanie 5-min braku połączenia + ufw).
