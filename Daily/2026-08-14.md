---
date: 2026-08-14
tags: [daily]
---

# 2026-08-14

Pełne podsumowanie dnia: [[Lab/Recap 2026-08-14]] · indeks: [[Dashboard]] · dziennik: [[Dziennik_Lab]]

## Auto-YARA + dashboard

`pipeline.sh --pe-only /root/samples/quarantine/` (22:15 UTC) — 3 reguły, 15 IOC. Dashboard `127.0.0.1:8766`. [[Dashboard_IOC]]

## Analiza

- SSH na VPS `5.175.189.133` (root) — Ubuntu 24.04, host `vserver959630`.
- Odpalony pipeline (`FORCE=1`) na PE z kwarantanny.
- Pipeline zakończony: static PE + YARA + strings. capa puste.
- Założony vault Obsidian: `/root/obsidian-vault`.
- Binarnego Obsidiana nie ma (`/opt/Obsidian` usunięty; został tylko profil AppArmor). Vault jest gotowy do otwarcia jako folder.

## Próbka dnia

[[Analizy/Malware/178cb931 Precision Agriculture Go PE]] — Go 1.25.4 GUI PE32+, motyw „Precision Agriculture / elevator”.

Werdykt roboczy: **Go Windows GUI z dekoracją ag/elevator**. YARA `IsPacked` raczej FP (wysoka entropia `.rdata` typowa dla Go). Brak URL/IP w stringach. Są API administracyjne (`LogonUserW`, `NetUserAdd`, `ImpersonateLoggedOnUser`) — do weryfikacji w Ghidra/x64dbg na `.57`, bez sieci.

## IoC

Zobacz [[Analizy/IOC/178cb931]].

## Higiena lab

Na hoście analizy nasłuchuje `python3 -m http.server 8765` z `/tmp/pe_in` (kopia próbki jako `backdoor.exe`). To publiczny hosting PE — do wyłączenia, nie do używania.

## Klasyfikacja korpusu

Cały zestaw z pipeline rozpisany: RAT (`kira.malware`), stealery (SMS, NFC, Chrome bank, .NET), backdoor Go, droppery, packed. **Cryptojacking: 0 potwierdzonych.** [[Klasyfikacja_Korpus]]

## Auto-log vault na `.133` (23:59 UTC)

- Sync Kali → `/root/obsidian-vault`
- Katalogi `Logs/` `Raporty/` `Screenshots/`
- `.bashrc`: `script` tylko dla interaktywnego TTY
- `pipeline.sh` woła `Narzedzia/export_pipeline_to_obsidian.sh`
- Cron `obsidian-sliver-log` — journal, bez zmiany `sliver.service`
- Bot Telegram dalej `active` / `enabled`, `VAULT_PATH=/root/obsidian-vault`

Zobacz [[Obsidian_Auto_Log]].

## Telegram bot

Bot `@Xmaskapp_bot` na VPS (systemd `obsidian-telegram-bot`, 24/7).  
`/dashboard` czyta pipeline i opisuje wirusa. `/xmask` daje posty na kanał. [[Telegram_Obsidian_Bot]] · [[XMask/README]]

## Next

1. `go tool nm` / symbole z `.symtab` (2613 symboli) — lista `main.*`.
2. Na `.57`: PEStudio + x64dbg (offline), potwierdzić czy `LogonUserW`/`NetUserAdd` są żywe czy tylko `x/sys`.
3. Overlay 2408 B — hexdump, czy to certyfikat (YARA `HasDigitalSignature` vs brak `DIRECTORY_ENTRY_SECURITY`).
4. capa z dłuższym timeoutem albo ręcznie.
5. Nie ruszać Sliver/C2 z tej sesji analizy.
